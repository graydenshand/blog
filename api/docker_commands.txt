## Run flask app in development mode, PWD must be this folder (/blog/api/)
docker run -it --rm \
 -v $PWD:/home/api/:rw \
 -p 5001:5000 \
blog_api:development

# Build development container
docker build . -t blog_api:development